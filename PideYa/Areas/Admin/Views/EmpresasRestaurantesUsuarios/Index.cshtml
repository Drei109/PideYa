@model IEnumerable<PideYa.Models.empresa_restaurante_usuario>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.empresa.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.restaurante.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.usuarioASP_fk_Id)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.empresa.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.restaurante.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.usuarioASP_fk_Id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.restaurante_usuario_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.restaurante_usuario_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.restaurante_usuario_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
