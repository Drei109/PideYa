@model PideYa.Models.tipo_restaurante

@{
    ViewBag.Title = "Tipos de Restaurante | Detalles";
}
<div class="row">
    <div class="col-lg-12">
        <!--begin::Portlet-->
        <div class="m-portlet">
            <div class="m-portlet__head">
                <div class="m-portlet__head-caption">
                    <div class="m-portlet__head-title">
                        <span class="m-portlet__head-icon m--hide">
                            <i class="la la-gear"></i>
                        </span>
                        <h3 class="m-portlet__head-text">
                            TIPOS DE RESTAURANTE
                        </h3>
                    </div>
                </div>
            </div>
            <!--begin::Form-->
            @using (Html.BeginForm("Details", "TiposRestaurante", FormMethod.Post, new { @class = "", role = "form" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                @Html.HiddenFor(m => m.tipo_restaurante_id)
                <div class="m-portlet__body">
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.LabelFor(m => m.descripcion, "Nombre:")
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.descripcion)
                        </dd>
                    </dl>
                </div>

                <div class="m-portlet__foot">
                    <div class="row">
                        <div class="col-lg-2"></div>
                        <div class="col-lg-6">
                            <a href="/Admin/TiposRestaurante/Index" class="btn btn-secondary">
                                Regresar
                            </a>
                        </div>
                    </div>
                </div>
            }
            <!--end::Form-->
        </div>
        <!--end::Portlet-->
    </div>
</div>
