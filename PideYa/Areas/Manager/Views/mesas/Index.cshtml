@model IEnumerable<PideYa.Models.mesa>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Manager/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mesa_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.restaurante.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th>
            Acciones
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.mesa_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.restaurante.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.mesa_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.mesa_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.mesa_id })
        </td>
    </tr>
}

</table>
